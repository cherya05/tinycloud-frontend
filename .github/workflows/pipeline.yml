name: Tinycloud Frontend Pipeline
on:
    push:
        branches:
            - main
env:
    AWS_REGION: eu-north-1
    FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
concurrency:
    group: tinycloud-frontend-pipeline
    cancel-in-progress: true
jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            - name: Configure AWS credentials
              uses: aws-actions/configure-aws-credentials@v5
              with:
                aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
                aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
                aws-region: ${{ env.AWS_REGION }}
            
            - name: Login to Amazon ECR
              id: login-ecr
              uses: aws-actions/amazon-ecr-login@v2

            - name: Build and Push Docker image
              run: |
                docker build --no-cache -t tinycloud-frontend:latest .
                docker tag tinycloud-frontend:latest ${{ steps.login-ecr.outputs.registry }}/tinycloud-docker-frontend:${GITHUB_SHA::7}
                docker push ${{ steps.login-ecr.outputs.registry }}/tinycloud-docker-frontend:${GITHUB_SHA::7}

            - name: Build and Push Helm chart
              run: |
                helm package charts/tinycloud-frontend
                helm push tinycloud-frontend-*.tgz oci://${{ steps.login-ecr.outputs.registry }}

    deploy:
        runs-on: ubuntu-latest
        needs: build
        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            - name: Configure AWS credentials
              uses: aws-actions/configure-aws-credentials@v5
              with:
                aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
                aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
                aws-region: ${{ env.AWS_REGION }}

            - name: Login to Amazon ECR
              id: login-ecr
              uses: aws-actions/amazon-ecr-login@v2

            - name: Setup Kubeconfig
              run: |
                echo "${{ secrets.KUBECONFIG }}" > kubeconfig.yaml
                echo "KUBECONFIG=$(pwd)/kubeconfig.yaml" >> $GITHUB_ENV

            - name: ECR Secret for Kubernetes
              run: |
                kubectl create secret docker-registry ecr-registry-secret \
                  --docker-server=${{ steps.login-ecr.outputs.registry }} \
                  --docker-username=AWS \
                  --docker-password=$(aws ecr get-login-password --region ${{ env.AWS_REGION }}) \
                  -n tinycloud-dev \
                  --dry-run=client -o yaml | kubectl apply -f -
            
            - name: Deploy to Civo
              run: |
                helm upgrade --install tinycloud-frontend-dev \
                  oci://${{ steps.login-ecr.outputs.registry }}/tinycloud-frontend \
                  --version 0.1.0 \
                  --set image.tag=${GITHUB_SHA::7} \
                  -f charts/tinycloud-frontend/values-dev.yaml \
                  -n tinycloud-dev \
                  --create-namespace