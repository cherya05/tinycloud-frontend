apiVersion: v1
kind: Service
metadata:
  name: {{ include "tinycloud-frontend.fullname" . }}
  labels:
    {{- include "tinycloud-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    {{- include "tinycloud-frontend.selectorLabels" . | nindent 4 }}